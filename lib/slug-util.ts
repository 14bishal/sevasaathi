/** Pure slug helpers — no DB dependency. Safe to import anywhere. */

export function buildBaseSlug(name: string): string {
  return name
    .toLowerCase()
    .trim()
    .replace(/\s+/g, '-')
    .replace(/[^a-z0-9-]/g, '')
    .replace(/-+/g, '-')
}
